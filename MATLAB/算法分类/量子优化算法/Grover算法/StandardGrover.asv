%标准Grover算法成功概率与π/2相移算法的成功概率对比
clear all;
close all;
%标记态与状态总数的比值
t = 0.001:0.001:1;
%标准Grover算法迭代步数
r = round(acos(sqrt(t))./(2*asin(sqrt(t))));
%标准Grover算法的成功概率
P = sin((2*r+1).*asin(sqrt(t))).^2;
%